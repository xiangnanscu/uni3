local template = require("resty.template")
local utils = require("xodel.utils")
local env = utils.getenv()

local auto_generate_conf_hint = [[
## this file is generated by bin/make_conf.lua, don't modify it.
## modify conf/nginx.template.conf, .env and .env.local instead
]]
print(auto_generate_conf_hint .. template.new('./conf/nginx.template.conf'):process(env))
